# VibeProof Canva Cover Brief

Status: prepared only. Do not create or publish a Canva design without explicit approval.

## Purpose

Create submission support visuals for the bounty reply and social sharing. These assets support the proof pack; the core proof remains the working app, source, screenshots, `/#proof`, and `npm run proof:audit`.

## Source Assets

- `delivery/vibeproof/vibeproof-desktop-proof.png`
- `delivery/vibeproof/vibeproof-tablet-proof.png`
- `delivery/vibeproof/vibeproof-mobile-proof.png`
- `delivery/vibeproof/vibeproof-proof-brief-desktop.png`
- `delivery/vibeproof/vibeproof-proof-brief-mobile.png`

## Required Exports

1. Bounty cover
   - Size: `1920 x 1080`
   - Use: Pump.fun reply / project preview

2. Square proof card
   - Size: `1080 x 1080`
   - Use: social/share preview

3. Mobile proof story
   - Size: `1080 x 1920`
   - Use: vertical proof recap

## Cover Copy

Headline:

```text
VibeProof Studio
```

Subheadline:

```text
Pure-local AI app builder in one browser tab
```

Proof chips:

```text
WebLLM local inference
LocalKit + PGlite backend
68 local tools
0 cloud AI / prompt API requests
PWA after first cache
```

Footer honesty note:

```text
First model load may download WebLLM/model assets. Prompts, generated files, toolbox input, and preview data stay out of hosted AI APIs.
```

## Composition

- Use the desktop workspace screenshot as the main visual.
- Use mobile and tablet screenshots as smaller overlapping device cards.
- Add a small audit badge: `npm run proof:audit: pass`.
- Keep background calm and glass-like, with light translucency and restrained depth.
- Avoid fake Apple logos, fake OS labels, brand marks, celebrity names, or platform claims that cannot be verified.
- Do not use generic AI gradients, blobs, or bokeh.

## Suggested Canva Prompt

```text
Create a premium product proof cover for "VibeProof Studio", a pure-local browser AI app builder. Use the provided real screenshots as primary evidence. The design should feel Apple-platform inspired with translucent glass panels, subtle rim lighting, compact status chips, and a clear proof-first hierarchy. Include proof chips for WebLLM local inference, LocalKit + PGlite backend, 68 local tools, 0 cloud AI / prompt API requests, and PWA after first cache. Add a small honesty note that first model load may download model assets, while prompts and generated code stay out of hosted AI APIs. Do not use Apple logos, fake OS branding, stock people, decorative blobs, or unverifiable claims.
```

## Review Checklist

- The working product screenshot is the main visual, not decoration.
- Text is readable at thumbnail size.
- The cover does not imply official Apple affiliation.
- The cover does not imply full offline model availability before first model download.
- The final export includes the honesty note or a visibly equivalent short version.
